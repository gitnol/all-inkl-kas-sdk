# All-Inkl KAS API Credentials
# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# Your KAS login (e.g. w0123456)
KAS_LOGIN=w0123456

# Your KAS password (plain text)
KAS_PASS=your_password_here

# Authentication type: "plain" (default) or "sha1"
# KAS_AUTH_TYPE=plain

# Set to 1 to enable dry-run mode (no API writes, reads still work)
# KAS_DRY_RUN=0
